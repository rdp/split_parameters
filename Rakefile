require 'jeweler'

require 'jeweler'
  Jeweler::Tasks.new do |s|
    s.name = "split_parameters"
    s.summary = "Ruby library to allow for objective-c like \"split\" parameters in a method call"
    s.email = "rogerdpack@gmail.com"
    s.homepage = "http://github.com/rdp/split_parameters"
    s.authors = ["Roger Pack"]
    s.add_development_dependency 'rspec'
    s.add_development_dependency 'sane'
    s.add_dependency 'rdp-arguments'
  end